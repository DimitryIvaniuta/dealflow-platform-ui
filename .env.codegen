# Used by npm run schema:download
VITE_API_BASE_URL=http://localhost:8080

# Option A: provide a ready JWT (fast)
# DF_TOKEN=...

# Option B: auto-login to obtain a token and introspect the schema
DF_USERNAME=admin
DF_PASSWORD=NewStrongP@ssw0rd!
